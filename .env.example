# Exemple de configuration (ne pas commiter de secrets)
# Copiez ce fichier en .env et remplissez les valeurs

# Obligatoires (renseignez vos valeurs)
SUPABASE_URL=
SUPABASE_KEY=

# Optionnels
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Utiliser soit VISION_TEXT (inline), soit VISION_FILE (chemin)
VISION_TEXT=
VISION_FILE=./scripts/vision_seeg.md

# SQLite local (utilisé par défaut pour persister certains scores/RAG)
# Le fichier est monté via docker-compose: ./scores.db:/app/scores.db

# RAG (Chroma + sentence-transformers)
ST_MODEL=sentence-transformers/all-MiniLM-L6-v2
CHROMA_DIR=/app/chroma
